apiVersion: v1
kind: Service
metadata:
  name: lab-nginx
  labels:
    app: lab-nginx
spec:
  type: NodePort
  selector:
    app: lab-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
